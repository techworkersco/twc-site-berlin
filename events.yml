---
layout: null
---
{% for event in site.events %}
- title: "Berlin: {{ event.title }}"
  date: {{event.date}}
  locations: [Berlin]
  tags: {{event.tags}} 
  timeszones: [Europe/Berlin]
  image: {{ event.image | absolute_url }}
  url: {{site.url}}{{event.url}}
{% endfor %}
