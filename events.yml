---
layout: null
---
{% for event in site.events %}
- title: "{{event.title}}"
  date: {{event.date}}
  locations: [Berlin]
  tags: {{event.tags}} 
  timeszones: [Europe/Berlin]
  image: {{site.url}}{{event.image}}
  url: {{site.url}}{{event.url}}
{% endfor %}